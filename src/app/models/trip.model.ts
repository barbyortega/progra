// PickMeUp/src/app/models/trip.model.ts
export interface Trip {
    id: number;       // ID del viaje
    route: string;    // Ruta del viaje
    fare: string;     // Tarifa del viaje
    driverName: string; // Nombre del conductor (si es relevante)
    
  }
  